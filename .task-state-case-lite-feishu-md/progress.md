# Progress Log

## 2026-07-25

- Completed browser OAuth for `feishu-docx-blocks` using the application configuration already present in the local Claude MCP setup. Stored credentials and tokens only in `~/.config/feishu-docx-blocks/.env`.
- Verified the supplied Wiki URL resolves to `个人错题本-服务端设计.md`, a native Drive `file`; the MCP downloaded it as UTF-8 Markdown and returned a requested original section (35,595 characters).
- Added regression coverage for cached refresh-token lookup, opaque token expiry fallback, and API error `99991668` as an invalid access token. Targeted MCP tests: 16 passed.
- Tightened the native Markdown directory call to return only headings and line ranges by default. The caller must pass `preview_chars` explicitly to obtain any preview; after selection it receives only the selected original Markdown. Targeted MCP tests: 17 passed.
- Found four existing MCP server processes from Cursor, Claude Code, and Codex sharing the same user-level token file. A later process rewrote the freshly authorized long token with a stale invalid one, so persistent OAuth validation is blocked until those processes are restarted or isolated.
- After the user restarted the MCP, a fresh process successfully completed the real two-step contract: 38 metadata-only headings from the supplied native Markdown file, then one selected original Markdown section (35,595 characters). No preview or unselected content was returned in directory mode.
- Built `feishu-docx-blocks` 3.4.0 sdist and wheel, passed Twine metadata checks, and clean-installed the wheel in an isolated Python 3.13 virtual environment. The installed package reports version 3.4.0 and registers `get_markdown_file_sections`.
- ClaudeCode completed the bounded read-only release review with `APPROVE WITH NITS`, one successful attempt, and provider-reported cost $1.56871475. Applied P3 findings: the model-facing tool description and plan now state metadata-only default output, and direct regression tests cover parent/child de-duplication and unknown section IDs. Final MCP suite: 19 passed; case-lite suite: 24 passed; final 3.4.0 artifacts pass Twine checks.
- Verified PyPI still serves 3.3.0 and has no 3.4.0 release. Attempted the authorized 3.4.0 upload once with the configured token; PyPI returned HTTP 403. The credential format is valid-looking but lacks effective upload authorization, so publication remains blocked and was not retried.
- After the PyPI credential was renewed, successfully published `feishu-docx-blocks` 3.4.0. A clean virtual environment installed the public PyPI wheel and completed the real supplied file's metadata-only directory (38 sections) and selected original Markdown (35,595 characters) flow.
- Fast-forwarded both feature branches into `SkillCollections/main` and `FeishuMCP/master`. Synchronized the four changed case-lite files to `~/.cc-switch/skills/case-lite`; its 24 installed contract tests pass and its source tree matches the project skill except cache directories.
