# Snapshot: 2026-07-25 11:14

## Context
The 403 root cause is isolated: the configured gateway rejects Claude Code print mode's official `sdk-cli` User-Agent, not the credential or Codex subprocess environment.

## Recent Progress
- Captured the actual Claude CLI request locally; its bearer-header hash exactly matches the configured token.
- Proved removing all `CODEX_*` variables does not change the request.
- Proved with no-model invalid-body requests that `sdk-cli` gets 403 while a truthful plain `claude-cli/2.1.216` identity reaches 400 request validation.
- Proved changing auth to `x-api-key` does not fix the `sdk-cli` rejection.

## Current Focus
Resolve the provider's client-identity policy without impersonating `claude-vscode`.

## Blockers
- Provider-side allowlist access is not available from this repository.
- A real review verification still needs an explicit user-approved per-call budget.

## Next Session Should Know
- Preferred fix: configure the gateway to allow the official `sdk-cli` User-Agent.
- Compatibility alternative: one child-only truthful plain Claude CLI User-Agent, only after explicit approval; never set `claude-vscode` from Codex.
- Use a fresh artifact key for verification so historical failed attempts remain intact.
